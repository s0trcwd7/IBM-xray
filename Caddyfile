:443 {
   
   tls /usr/local/bin/cert.crt /usr/local/bin/cert.key
   
   @xray_vl_websocket {
      path /${XR_VL_UUID}-vless
      header Connection *Upgrade*
      header Upgrade websocket
   }
   reverse_proxy @xray_vl_websocket :8080
}
